# Walidacja geometrii

Sprawdź manifoldness, normalne, duplikaty, samoprzecięcia, skalę, orientację osi i spójność topology freeze.

Dla eksportów dodatkowo raportuj liczbę vertices, triangles, submeshes i materiałów oraz bounding box. Nagły skok tych wartości pomiędzy wersjami powinien generować ostrzeżenie w Avatar Studio.