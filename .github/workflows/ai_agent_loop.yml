name: AI Agent Task Loop

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Workflow intentionally manual
        run: |
          echo "AI automation is not enabled yet."
          echo "This placeholder does not call external APIs or modify the repository."
